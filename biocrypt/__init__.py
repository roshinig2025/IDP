"""Bio-Crypt Lock — Risk-Adaptive Multi-Factor Authentication prototype.

Packages: engine (risk fusion, tier routing, TOTP), crypto (AES-256-GCM
vault), audit (hash-chained SQLite logger), ui (tkinter dashboard +
simulated BLE trusted device).
"""

__version__ = "1.0.0"
